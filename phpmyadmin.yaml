apiVersion: apps/v1
kind: Deployment
metadata:
  name: phpmyadmin
  namespace: fullstackprog
spec:
  selector:
    matchLabels:
      app: phpmyadmin
  template:
    metadata:
      labels:
        app: phpmyadmin
    spec:
      containers:
        - name: phpmyadmin
          image: phpmyadmin:5.2
          env:
            - name: PMA_HOST
              value: db
            - name: MYSQL_ROOT_PASSWORD
              value: rootpass
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: phpmyadmin-service
  namespace: fullstackprog
spec:
  selector:
    app: phpmyadmin
  ports:
    - port: 80
